<?xml version="1.0" encoding="utf-8"?>
<!-- AndroidManifest.xml
     声明 APP 组件、权限、硬件/软件特性。

     切换为 HTTP 远程加载模式：
     不再从本地 assets/hermes/ 加载前端，改为直接加载用户配置的
     服务器 URL。所有 API 请求同源，无需任何跨域注入或权限。

     - INTERNET 权限：WebView 访问网络必需
     - usesCleartextTraffic：允许连接 HTTP 局域网服务器
     - configChanges：横竖屏切换不重建 Activity
     - hardwareAccelerated：硬件加速提升 WebView 渲染性能 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络访问权限（WebView 加载页面和 API 通信必需） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 网络状态访问权限（用于检测网络变化） -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HermesStudioMobile"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- LAUNCHER：桌面图标入口 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 服务器配置界面 -->
        <activity
            android:name=".ConfigActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>